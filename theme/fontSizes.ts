const fontSizes = {
    subtitle: 10, // Chữ rất nhỏ
    xs: 12,       // Text phụ
    sm: 14,       // Nút "Skip"
    md: 16,       // Body text / Mô tả
    lg: 18,       // Chữ trong nút "Next"
    xl: 22,
    title: 28,    // Tiêu đề "Learn IT Vocabulary" (Tăng nhẹ từ 26 -> 28 cho rõ)
    huge: 32,     // Dự phòng nếu cần to hơn
};

export default fontSizes;