const radius = {
    sm: 8,   // Bo góc nhẹ
    md: 12,  // Bo góc chuẩn cho nút (Button Next)
    lg: 20,
    xl: 30,  // Bo góc lớn
    circle: 100, // Dùng cho Icon tròn gradient ở giữa
};

export default radius;